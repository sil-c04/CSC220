Solution to LeetCode Problem #164

This code returns the maximum difference between two successive elements in a sorted array.

The array is sorted using bucket sort with a linear time complexity of O(n).

